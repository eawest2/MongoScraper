        <div  class="card">
            <div class="card-header">
            </div>
            <div class="card-body">
                <h3>{{headline}}</h3>
                <h4>{{body}}</h4>
<a class="btn btn-danger save" id="deleteNote" data-id="{{id}}">Delete Note</a>
            </div>
        </div>